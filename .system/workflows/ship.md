---
description: Deterministic Git Push - Fast, transparent, and atomic.
---

# SHIP WORKFLOW (Fast GitHub Sync)

Use this to quickly push your changes to the remote repository.

// turbo-all
1. **Stage**: `git add .`
2. **Commit**: `git commit -m "feat: gromada engineering standard & phase templates"`
3. **Push**: `git push origin main`

## Pre-flight check
- Ensure no merge conflicts exist.
- Ensure all tests passed (optional for hot-sync, recommended for release).
